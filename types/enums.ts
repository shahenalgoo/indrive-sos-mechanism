export enum ThirdPartyHelp {
    police = "911",
    contacts = "Trusted Contacts"
}

export enum Role {
    client = "client",
    agent = "agent"
}

export enum Mood {
    neutral = "Neutral",
    happy = "Happy",
    sad = "Sad"
}