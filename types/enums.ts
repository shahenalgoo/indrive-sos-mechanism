export enum Severity {
    minor = "Minor",
    moderate = "Moderate",
    critical = "Critical"
}

export enum ThirdPartyHelp {
    police = "911",
    contacts = "Trusted Contacts"
}

export enum Role {
    client = "client",
    agent = "agent"
}