// Third party help options
export enum ThirdPartyHelp {
    police = "911",
    contacts = "Trusted Contacts"
}

// User Role
export enum Role {
    client = "client",
    agent = "agent"
}

// User Mood
export enum Mood {
    neutral = "Neutral",
    happy = "Happy",
    sad = "Sad"
}