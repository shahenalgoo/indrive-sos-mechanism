export enum Severity {
    minor = "Minor",
    moderate = "Moderate",
    critical = "Critical"
}

export enum ThirdPartyHelp {
    police = "911",
    contacts = "Trusted Contacts"
}

export enum Role {
    client = "client",
    agent = "agent"
}

export enum Mood {
    neutral = "Neutral",
    happy = "Happy",
    sad = "Sad"
}